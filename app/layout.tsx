import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TerraQuest - Interactive 3D Terraform Learning Platform",
  description: "Master Terraform through an immersive 3D journey. Explore 9 interactive learning regions, complete challenges, and track your progress in this gamified Infrastructure as Code education platform.",
  icons: {
    icon: '/terraform.svg',
    apple: '/terraform.svg',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
